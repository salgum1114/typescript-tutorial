import * as React from 'react';
import { Button, Input } from '../lib';

class App extends React.Component {
    render() {
        
        return (
            <div>
                <Button>test</Button>
                <Input />
            </div>
        );
    }
}

export default App;